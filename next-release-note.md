>更新内容记录在此处，每次更新release时作为releasenote发布，发布后清理已发布内容。

## 新增

- **Windows 有安装包了。** 以前 Windows 用户得自己装 Rust 编译，现在去 Releases 页下载就行：
  - `MiyuSetup-<版本>-x64.exe`：装到 `%LOCALAPPDATA%\Programs\Miyu`，自动把 `miyu` 加进用户级 PATH，带卸载器，**免管理员**；安装时可以顺手勾上 PowerShell 终端集成。
  - `Miyu-<版本>-windows-x64.zip`：便携包，解压后把 `bin\` 加进 PATH 即可用。

  两个包都带上了中文字体、表情库与 `rg`（文件搜索用），新装机器开箱即用。升级安装会先停掉正在跑的后台进程（Windows 不允许覆盖运行中的程序），卸载**不会动** `%USERPROFILE%\.miyu` 里的配置、会话与记忆。图片与 mermaid 渲染仍需自备 `chafa`（缺了只是不画图，不影响其它功能）。

## 修复

- 同一个 Miyu 家目录不会再同时跑起两个后台进程。此前只要 `MIYU_HOME` 在一处设了、另一处没设（哪怕它指的就是默认的 `~/.miyu`），同一份数据就会被当成两套环境各起一个后台进程，连带各自拉起一个语音进程抢同一个麦克风，白占约 80MB 内存和 0.8% 的 CPU。现在后起的那个会主动让位；万一你两边的环境确实对不齐，命令行会直接说明哪个进程正占着、以及怎么把它交接过来，而不是卡在「启动超时」。

- **Windows：TUI 里一次按键不再被当成两次。** 方向键按一下光标动两格、直连 REPL 里打字会重复出字符——Windows 的终端会同时上报「按下」和「松开」两种按键事件，而这些界面此前只按事件个数走。现在设置界面（`miyu config`）、REPL 里的 `/effort` `/models` `/persona` 面板、模糊选择菜单、`/pop` 菜单与直连 REPL 都只认「按下」。

- **Windows：一次只跑一个后台进程重新生效。** 家目录单例锁这一版在上游是用 Unix 专有的文件锁调用写的，在 Windows 上根本编不过；现在改走 Miyu 自己的平台层，同一个家目录再起第二个会被正确挡下并告诉你是谁占着。

- **Windows：中转线的命令行进程收尾更干净。** claude-code / codex / antigravity 三条线的子进程退出时改走 Windows 的进程树终止（连同它自己拉起的子进程一起收），并且不再闪出控制台窗口。

- **同步上游 Miyu 0.6.1**：上游这次做的是内部结构重排（拆成多个 crate，不影响使用），同时带来 mermaid 代码块直接画成图（终端与 WebUI 都有）、设置界面整屏重画（「插件配置」与「自定义提示词」合并成「人格和功能」）、opencode Zen 免费档恢复可用、QQ 群聊触发与好感度一批修复等。细节见上游 0.6.1 更新记录。

## 修复（续）

- macOS 上编不过的一处修好了。`trim_process_memory` 这个函数在 Linux 和非 Linux 两支上可见性写得不一致，跨 crate 引用时在 macOS 编译直接失败——Linux 永远编得过，所以一直没人发现。同时给 CI 加了 macOS 这一条流水线（编译、语音那一面、单元测试），以后再往里带 Linux 专属假设，提交当场就会被拦下。
- macOS 上也不会再留下没人收尸的后台进程了。由启动者带起来的后台进程，在启动者被强杀之后会跟着退出——这件事 Linux 上早就做了，macOS 缺一个对应的内核机制，一直空着。
- 少装了东西时，报错会说清缺的是什么。以前在没有 ripgrep 的机器上，让她搜代码只会得到一句「No such file or directory (os error 2)」——连缺的是哪个程序都没说，她只能去猜路径、换参数，白白浪费几轮。现在直接说是哪个程序不在 PATH 上，以及怎么装。
- Arch 专属的那几件工具（AUR 查询/审查安装、Arch 新闻、ArchWiki）不再默认出现在非 Arch 系统上。以前无论 macOS 还是 Debian，这些工具都会挂在她的工具清单里白占额度，其中 AUR 那几件在那些系统上必然失败。现在默认跟着系统走；**想要的话在「人格和功能」里勾上照样能用**。已经在用的非 Arch 机器会自动关掉一次，需要的自己开回来。
- macOS 上那几个要浏览器的脚本（小红书、知乎、闲鱼检索）现在认得出系统里装的 Chrome 了。以前只在 PATH 上找，而 macOS 的浏览器装在应用包里、不在 PATH 上，于是明明装着 Chrome 也会被判成「没有可用浏览器」。
- 内置插件「API 额度查询」（`query_api_quota`，查 DeepSeek / OpenRouter 的账户余额）**退役了**：上游的判断是它只支持两家、不值一份常驻的工具契约，配置项一并去掉。已经在用的机器上这件工具会消失。供应商侧出问题时，现在有专门的「供应商状态排查」技能（问「模型报错 / DeepSeek 挂了吗」会自己加载）。想把余额查询做成按需加载的脚本，做法记在 `docs/plan/2026-09-21-windows-sync-followups.md`。
